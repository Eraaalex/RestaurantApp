version: '2'

services:
  db:
    image: 'postgres:latest'
    ports:
      - '5432:5432'
    environment:
     - POSTGRES_USER= postgres
     - POSTGRES_PASSWORD= eralex
     - POSTGRES_DB = restaurant_app_db
  app:
    build:
      context: .
    ports:
      - '8081:8081'

